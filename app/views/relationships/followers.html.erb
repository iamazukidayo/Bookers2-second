<h1>Follow Users</h1>

<% users.each do |user| %>
  <table>
    <thead>
      <tr>
        <th>name</th>
        <th></th>
        <th></th>
      </tr>
    </thead>
  </table>

  <tbody>
    <tr>
      <td><%= user.name %></td>
      <td>フォロー数: <%= user.followings.count %></td>
      <td>フォロワー数: <%= user.followers.count %></td>
    </tr>
  </tbody>
   <% else %>
  <p>ユーザーはいません</p>
 <% end %>
<% end %>
