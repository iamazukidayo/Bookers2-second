<table class='table'>
  <tr><%= image_tag user.get_profile_image(100,100) %></tr>
    <!--%= image_tag book.user.get_profile_image(100, 100) %></tr>-->
  <tr>
	  <th>name</th>
	  <th><%= user.name %></th>
  </tr>
  <tr>
	  <th>introduction</th>
	  <th><%= user.introduction %></th>
  </tr>
</table>


    <% if user.id == current_user.id %>
       <div><%= link_to edit_user_path(current_user.id), class: "edit btn btn-outline-secondary btn-block"  do %>
               <i class="fas fa-user-cog"></i>

            <% end %>
       </div>

        <% else %>
          <div><%= link_to edit_user_path(user.id), class: "edit btn btn-outline-secondary btn-block"  do %>
                <i class="fas fa-user-cog"></i>
               <% end %>
         </div>
        <% end %>


<!--<div class='row'>-->
  <!--%= link_to edit_user_path(current_user.id) do %>-->
  <!--<div class: "edit btn btn-outline-secondary btn-block" ><span class="glyphicon glyphicon-wrench" aria-hidden="true">-->
    <!--<i class="fas fa-user-cog"></i>-->
    <!--</span></div>-->
  <!--% end %>-->
 <!--</div>-->
